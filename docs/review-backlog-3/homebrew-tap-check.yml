# Install as homebrew-abyss/.github/workflows/kinjo.yml and require both jobs
# on protected tap main. This file is documentation until copied to the tap.
name: Check Kinjo formula

on:
  pull_request:
    paths:
      - Formula/kinjo.rb
      - .github/workflows/kinjo.yml
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: kinjo-formula-${{ github.ref }}
  cancel-in-progress: true

jobs:
  formula:
    name: Kinjo on ${{ matrix.arch }}
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: arm64
            os: macos-15
          - arch: x86_64
            os: macos-15-intel
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
        with:
          persist-credentials: false

      # brew audit and brew install no longer accept a file path; both require a
      # formula name resolved through a tap. Expose this checkout as the tap once
      # so the later steps can refer to abbyssoul/abyss/kinjo.
      - name: Register the checkout as a tap
        run: |
          tap_dir="$(brew --repository)/Library/Taps/abbyssoul/homebrew-abyss"
          mkdir -p "$(dirname "$tap_dir")"
          ln -sfn "$GITHUB_WORKSPACE" "$tap_dir"

      - name: Audit the formula
        run: brew audit --strict --online abbyssoul/abyss/kinjo

      - name: Install and execute the changed formula
        run: |
          expected="$(sed -nE 's|.*(/releases/download/)v([0-9]+\.[0-9]+\.[0-9]+)/kinjo-.*|\2|p' Formula/kinjo.rb | head -1)"
          test -n "$expected"
          brew install --formula abbyssoul/abyss/kinjo
          test "$(uname -m)" = "${{ matrix.arch }}"
          kinjo --version | grep -F "$expected"

